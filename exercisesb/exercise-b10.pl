#!/usr/bin/perl

use warnings;

#what will the following program print

@x = (3, 4, 8, 10);  # 4 values inside x array
foreach (@x)  # for each value inside this array do this
	{
	print ("hello\n");
	}
