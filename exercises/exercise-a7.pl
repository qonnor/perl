#!usr/bin/perl

use warnings;

#my_variables

$x = 3;
$y = 0;
$z = -4;

#problem a
$a = !( $x < $y );
print $a;
print "\n";

#problem b
$b = !( $x >= $y );
print $b;
print "\n";

#problem c
#$c = !( (4 + 5 * %y) >= $z -2 ) && ( $z - 2 );
#print $c;

#problem d
#$d = !($x) && !($y) || ! ($z);
#print $d;
